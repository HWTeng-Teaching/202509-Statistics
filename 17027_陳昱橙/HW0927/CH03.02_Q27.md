<img width="502" height="645" alt="image" src="https://github.com/user-attachments/assets/ce024bf6-9da3-4bb9-953c-ec064e303d9a" />

# 高樓大廈問題之統計分析 (GitHub 格式)

這份文件詳細解答了關於洛杉磯市中心大樓高度與樓層數關係的統計問題。

---

### a. 自變數 (Independent) 與應變數 (Dependent)

在建築學和常識中，建築的高度是由其樓層數決定的。你增加越多的樓層，建築就越高。

-   **自變數 (Independent Variable / X)**: **樓層數 (Floors)**，因為它是決定建築高度的原因。
-   **應變數 (Dependent Variable / Y)**: **高度 (Height)**，因為它的數值「取決於」樓層數。

---

### b. 相關係數 (r) 及其意義

相關係數 $r$ 用來衡量兩個變數之間線性關係的強度和方向。

-   **計算結果**: $r \approx 0.955$

-   **意義解讀**:
    -   **方向 (Direction)**: $r$ 值為**正數**，表示建築的樓層數和高度之間存在**正向關係**。也就是說，樓層數越多的建築，通常也越高。
    -   **強度 (Strength)**: $r$ 的值非常接近 +1，這表示它們之間的線性關係**非常強 (very strong)**。

---

### c. 散佈圖與模式分析

**這是數據的散佈圖，請依照文章開頭的教學操作，將其顯示在你的 GitHub 上。**

![Scatterplot of Building Height vs. Floors]([image-tag: code-generated-image-1-1760075251206366352])

**請用你自己的 GitHub Assets 網址替換下面的 `src` 連結：**
```html
<img width="700" alt="Scatterplot of Building Height vs. Floors" src="[https://github.com/user-attachments/assets/](https://github.com/user-attachments/assets/)........">
```

**圖表模式分析**:
1.  **模式 (Pattern)**: 散佈圖清晰地顯示了一個**非常強的正向線性模式**，再次驗證了 $r$ 值的結論。數據點緊密地聚集在一條上升的直線周圍。
2.  **離群值 (Outliers)**: 圖中**沒有明顯的離群值**。所有數據點都相當符合整體的趨勢，沒有任何一個點極端偏離這條隱形的直線。

---

### d. 最小平方法迴歸線與預測

最小平方法迴歸線 (least-squares line) 能最好地擬合數據，其方程式形式為：
$$\hat{y} = b_0 + b_1 x$$

根據計算，我們得到：
-   **截距 (Intercept, $b_0$)**: `-13.84`
-   **斜率 (Slope, $b_1$)**: `4.40`

因此，迴歸線方程式為：
$$\text{預測高度} = -13.84 + 4.40 \times \text{樓層數}$$

**預測一棟有 48 層樓的建築高度**:
將 `樓層數 = 48` 代入方程式：
$$\text{預測高度} = -13.84 + 4.40 \times (48)$$
$$\text{預測高度} = -13.84 + 211.2$$
$$\text{預測高度} \approx 197.36$$

**結論：根據此模型，我們預測一棟有 48 層樓的建築，其高度約為 197.4 公尺。**

---
### 附錄：用於分析的 Python 程式碼
<details>
<summary>點此展開程式碼</summary>

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.linear_model import LinearRegression

# 註：雖然題目說有28棟建築，但表格中只列出27組數據
data = {
    'height': [335, 310, 262, 229, 228, 224, 221, 220, 219, 213, 213, 206, 203, 197, 194, 190, 189, 185, 176, 174, 174, 163, 162, 162, 158, 157, 154],
    'floors': [73, 73, 62, 52, 52, 55, 53, 54, 52, 52, 52, 49, 54, 58, 49, 48, 42, 45, 42, 44, 44, 41, 39, 40, 37, 40, 36]
}
df = pd.DataFrame(data)

# b. 計算相關係數 r
correlation_r = df['floors'].corr(df['height'])
print(f"相關係數 (r): {correlation_r}")

# c. 繪製散佈圖
plt.figure(figsize=(10, 7))
sns.regplot(x='floors', y='height', data=df, ci=None, line_kws={"color": "red"})
plt.title('Building Height vs. Number of Floors')
plt.xlabel('Number of Floors')
plt.ylabel('Height (meters)')
plt.grid(True)
plt.savefig('scatterplot_buildings.png')
plt.close()
print("散佈圖 'scatterplot_buildings.png' 已儲存。")

# d. 找出最小平方法迴歸線並預測
X = df[['floors']]
y = df['height']

model = LinearRegression()
model.fit(X, y)

slope = model.coef_[0]
intercept = model.intercept_
print(f"迴歸線斜率: {slope}")
print(f"迴歸線截距: {intercept}")

# 預測 48 層樓的高度
floors_to_predict = 48
predicted_height = model.predict([[floors_to_predict]])[0]
print(f"預測 48 層樓的高度: {predicted_height}")
```
</details>
